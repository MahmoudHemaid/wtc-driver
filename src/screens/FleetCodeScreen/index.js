import {connect} from 'react-redux';
import FleetCodeScreen from './FleetCodeScreen';

const mapStateToProps = (state, ownProps) => {
  return {};
};
const mapDispatchToProps = (dispatch) => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(FleetCodeScreen);
